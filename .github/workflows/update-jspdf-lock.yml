name: Update jsPDF lockfile

on:
  push:
    branches:
      - feature/order-printing-escpos
    paths:
      - package.json
      - .github/workflows/update-jspdf-lock.yml

permissions:
  contents: write

jobs:
  update-lock:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feature/order-printing-escpos
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Update package lock
        run: npm install --package-lock-only --ignore-scripts

      - name: Commit lockfile
        run: |
          if git diff --quiet -- package-lock.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "chore: lock jsPDF dependency"
          git pull --rebase origin feature/order-printing-escpos
          git push origin HEAD:feature/order-printing-escpos
